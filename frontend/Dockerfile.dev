# Development Dockerfile for Nuxt 4 Frontend
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies (including dev dependencies)
RUN npm ci

# Note: Source code is mounted as volume in docker-compose.dev.yml
# This allows for hot reload during development

# Expose port
EXPOSE 8901

# Set environment variables for development
ENV HOST=0.0.0.0 \
    PORT=8901 \
    NODE_ENV=development

# Start development server with hot reload
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "8901"]
